from setuptools import setup, find_packages

setup(
    name="MediBot2.0",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here, e.g. 'requests', 'flask'
    ],
    author="Mohammadrahil Nasardi",
    description="A medical bot project.",
    python_requires=">=3.6",
)